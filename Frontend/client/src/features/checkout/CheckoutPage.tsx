import { Typography } from "@mui/material";

const CheckoutPage = () => {
  return (
    <Typography variant="h4">
        Only logged in users should be able to see Checkout page!
    </Typography>
  )
};

export default CheckoutPage;
